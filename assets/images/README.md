# Фото и видео — куда и как добавлять

8 слотов уже заполнены реальными фото (см. таблицу). Остались основатель, Премиум Юрта-Дом и баня.

Сырые материалы — в Google Drive (папка «фото/видео», ~150 HEIC/MOV файлов + архив «Ак Тенгир» на 109 МБ, который пока не удалось скачать — превышает лимит загрузки через Диск). Просмотрено порядка 60 файлов.

## Слоты на странице (атрибут `data-slot` в index.html)

| Слот | Секция | Статус | Файл |
|---|---|---|---|
| `hero` | Hero (главный экран) | ✅ Готово | `hero.jpg` — закатный пляж, юрты, горы |
| `about` | О месте | ✅ Готово | `about.jpg` — юрта с каменной дорожкой |
| `founder` | История и основатель | Плейсхолдер | Архивных кадров основателя не найдено — открытый вопрос из брифа |
| `room-standard` | Размещение — Юрта Стандарт | ✅ Готово | `room-standard.jpg` — вход в войлочную юрту, вид изнутри наружу |
| `room-vip` | Размещение — ВИП юрта | ✅ Готово | `room-vip.jpg` — шатёр с золотыми шторами и панорамным видом на озеро |
| `room-beach` | Размещение — Пляжная премиум юрта | ✅ Готово | `room-beach.jpg` — панорамный вид на озеро из юрты |
| `room-house` | Размещение — Премиум Юрта-Дом | Плейсхолдер | Экстерьер/интерьер не найден в просмотренной части |
| `territory` | Территория и активности | ✅ Готово | `territory.jpg` — лаунж у воды под навесом |
| `retreats` | Ретриты | ✅ Готово | `retreats.jpg` — вечерний ретрит-круг у костра, юрты на фоне |
| — | Питание | ✅ Готово | `food.jpg` — блок `.food-photo` в секции, домашний стол |

Отдельно: баня (`#banya`) — фото/видео по-прежнему не найдены нигде (открытый пункт в брифе), карта в разделе «Как добраться» — нужен embed по точному адресу.

⚠️ `room-standard.jpg` и `room-vip.jpg` — лучшие доступные кадры из присланных материалов, но это не буквально «спальня» каждой категории (интерьеров кроватей не нашлось): стандарт — вход в войлочную юрту, ВИП — шатёр-лаунж с видом на озеро, передающий уровень категории. Если попадутся точные фото номеров — легко заменить.

## Ещё варианты, отобранные, но не использованные

Есть запасные кадры на замену любого из готовых слотов (см. контактные листы, присланные в чат): ряд юрт с дрона (IMG_0135), одиночная юрта на закате с дорожкой (IMG_0956), ферма/огород (IMG_0852, 0866–0868), яблоневый сад (IMG_1679–1680), Будда в окне лаунжа (IMG_6930), вечерний костёр — ещё два похожих кадра (IMG_1317, IMG_1327). Если что-то из готового не понравится — легко поменять местами.

## Как подключить фото дальше

1. Положить оптимизированные файлы (jpg/webp, сжатые под веб) в эту папку с понятным именем, например `room-house.jpg`.
2. В `index.html` заменить соответствующий `<div class="media-placeholder" data-slot="...">...</div>` на `<img src="assets/images/<имя файла>" alt="...">` (для фото внутри `.about-media`, `.t-media`, `.f-media`, `.room-photo` стили уже подключены автоматически — см. `css/style.css`).
3. Для hero-фона используется `<img class="ph-bg kenburns" ...>` — уже готово, менять файл `hero.jpg` можно просто заменив содержимое.

Видео (MOV) перед использованием на сайте лучше сконвертировать в mp4/webm и сжать — исходники из Drive весят по 50–200 МБ.
